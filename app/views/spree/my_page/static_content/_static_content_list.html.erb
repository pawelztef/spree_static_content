<% if page.foreign_link.present? %>
  <%# <li><%= link_to page.title, page.foreign_link, target: '_blank', rel: 'nofollow' %1></li> %>
<% else %>
  <%# <% page_uri = Rails.application.routes.named_routes[:spree].path.spec.to_s == '/' ? page.slug : Rails.application.routes.named_routes[:spree].path.spec.to_s + page.slug %1> %>
  <%# <li class="<%= 'current' if (request.fullpath.gsub('//','/') == page_uri) %1>"><%= link_to page.title, page_uri %1></li> %>
  <li><%= link_to page.title, my_page_static_path(page.slug) %></li>
<% end %>
