<% content_for :page_title do %>
  <div class="page-title-wrp">
    <%= Spree.t(:editing_resource, resource: Spree::Page.model_name.human) %>: <%= @page.title %>
    <span class="text-muted">(<%= Mobility.locale %>)</span>
  </div>
<% end %>

<%= render 'form', resource: @page %>
